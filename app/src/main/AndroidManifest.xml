<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.paigesoftware.materialdesigncomponents">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialDesignComponents">
        <activity android:name=".topappbar.TopAppBarActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar"/>
        <activity android:name=".datepicker.DatePickerActivity" />
        <activity android:name=".bottomsheet.BottomSheetActivity" />
        <activity
            android:name=".bottomappbar.BottomAppBarActivity"
            android:theme="@style/Theme.MaterialComponents.Light.NoActionBar" />
        <activity android:name=".floatingbutton.FloatingButtonActivity" />
        <activity android:name=".alertdialog.AlertDialogActivity" />
        <activity android:name=".snackbar.SnackBarActivity" />
        <activity android:name=".bottomnavigationview.BottomNavigationViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>